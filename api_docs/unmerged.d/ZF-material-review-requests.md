**Feature level change:** Generated Hover messages can now include field-scoped
`disputed_details`, and native assistant messages can include
`hover_review_request` metadata. Material ambiguity uses native silent mentions
for attention while any confirmed Space member retains Review permission. A new
server-owned disputed-detail evidence endpoint resolves only the exact conflicting
Evidence Links.
