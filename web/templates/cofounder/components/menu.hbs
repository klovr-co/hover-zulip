<div{{#if id}} id="{{id}}"{{/if}} class="cf-menu{{#if custom_classes}} {{custom_classes}}{{/if}}" data-simplebar data-simplebar-tab-index="-1">
    <ul role="menu" class="cf-menu__list"{{#if aria-label}} aria-label="{{aria-label}}"{{/if}}{{#if aria-labelledby}} aria-labelledby="{{aria-labelledby}}"{{/if}}>
        {{> @partial-block .}}
    </ul>
</div>
