<span class="cf-status{{#if tone}} cf-status--{{tone}}{{/if}}{{#if custom_classes}} {{custom_classes}}{{/if}}"{{#if aria-label}} aria-label="{{aria-label}}"{{/if}}>
    {{#if icon}}
    {{> icon name=icon compact=true }}
    {{/if}}
    <span>{{label}}</span>
</span>
