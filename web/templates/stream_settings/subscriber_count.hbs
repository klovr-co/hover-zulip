{{> ../cofounder/components/icon name="users" compact=true }}
{{#if can_access_subscribers}}
<span class="subscriber-count-text">{{numberFormat subscriber_count}}</span>
{{else}}
{{> ../cofounder/components/icon name="lock" compact=true custom_classes="subscriber-count-lock" }}
{{/if}}
